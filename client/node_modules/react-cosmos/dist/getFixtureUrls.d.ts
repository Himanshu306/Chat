import { CosmosConfig } from './config';
declare type Args = {
    cosmosConfig: CosmosConfig;
    fullScreen?: boolean;
};
export declare function getFixtureUrls({ cosmosConfig, fullScreen }: Args): Promise<string[]>;
export {};
